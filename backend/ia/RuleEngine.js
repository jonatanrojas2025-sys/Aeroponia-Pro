/**
 * ==========================================================
 * FDC124 - RuleEngine
 * ----------------------------------------------------------
 * Motor encargado de evaluar cada variable del cultivo.
 *
 * Este módulo NO calcula ISSA.
 * Este módulo NO genera diagnósticos.
 * Este módulo NO imprime información.
 *
 * Únicamente evalúa el estado de cada variable utilizando
 * la Base de Conocimiento.
 * ==========================================================
 */

class RuleEngine {

    evaluar(lectura, conocimiento) {

        const resultados = [];

        // Aquí se evaluará cada variable
        // (se implementará progresivamente)

        return resultados;

    }

}

module.exports = new RuleEngine();